# Lightweight requirements for Vercel deployment with OpenAI API
# Removes heavy ML dependencies (torch, whisper, etc.)

fastapi==0.115.12
uvicorn==0.34.3
python-multipart==0.0.20
pydantic==2.11.5
pydantic-settings==2.9.1
filetype==1.2.0
boto3==1.34.0
openai==1.60.1

# Keep existing utilities
# Note: SubsAI and torch dependencies removed for serverless compatibility